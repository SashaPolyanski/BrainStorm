import React from 'react';

import Button from '../components/button/Button';

const Login = () => {
  const inc = 12;

  return (
    <div>
      <Button variant="auth" name="AUTH" />
    </div>
  );
};

export default Login;
